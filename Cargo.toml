[workspace]
members = ["core", "cli", "gtk"]
default-members = ["cli", "gtk"]
resolver = "2"

[patch.crates-io]
libghostty-vt-sys = { path = "vendor-libghostty-vt-sys" }

[profile.dist]
inherits = "release"
lto = "thin"
